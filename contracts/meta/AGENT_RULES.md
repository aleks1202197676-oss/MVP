# AGENT RULES

- Use deterministic local runs first.
- Keep `python -m pipelines.run_all` backward-compatible.
- Update `contracts/meta/project_state.yml` after each green run.
- Ensure `latest_bundle.zip` always contains Memory Pack and finance contract files.
