# START HERE

## For AI agents
1. Read `AGENT_RULES.md`.
2. Read `project_state.yml`.
3. Read `roadmap.yml`.
4. Execute `python -m pipelines.run_all` and continue from `next_step`.

## For humans
- Main entrypoint: `python -m pipelines.run_all`
- Finance only: `python -m pipelines.finance.run_finance`
- Attach to a new chat: `data/ai_contract/latest/latest_bundle.zip`.
